const user = { name: 'Mark Laurence', age: 24 };

console.log('Mark Laurence');

// we know it dont exist in user object but it will still run in normal js and wont show red line under the code.
console.log(user.email);
